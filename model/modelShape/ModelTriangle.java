package model.modelShape;

import java.util.List;

import javafx.scene.paint.Color;

public class ModelTriangle extends ModelShape {

	protected ModelTriangle(double x, double y, double width, boolean mouvementDirection, boolean acceleration, Speed s,
			List<Color> colors, int pos_color) {
		super(x, y, width, mouvementDirection, acceleration, s, colors, pos_color);
		// TODO Auto-generated constructor stub
	}

}
